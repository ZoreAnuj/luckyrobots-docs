# Normalize line endings: LF in the repo, native on checkout.
* text=auto eol=lf

# PowerShell helper scripts use CRLF locally (Windows).
*.ps1 text eol=crlf

# Treat these as binary (no diff/merge noise).
*.png binary
*.jpg binary
*.ico binary
