# Lucky Engine docs site — Python toolchain
# Install into a venv:  python -m venv .venv && .venv\Scripts\pip install -r requirements.txt

mkdocs-material>=9.5,<10          # theme + mkdocs itself
mike>=2.1,<3                       # versioned docs (the 2026.1 <-> 2026.2 dropdown)
mkdocs-awesome-pages-plugin>=2.9,<3   # auto-builds nav from the docs/ tree + .pages files
pymdown-extensions>=10            # superfences / snippets / tabbed (pulled in by material; pinned for reproducibility)
